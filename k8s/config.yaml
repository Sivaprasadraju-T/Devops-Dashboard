apiVersion: v1
kind: ConfigMap

metadata:
  name: devops-dashboard-config
  namespace: devops-dashboard

data:
  ENVIRONMENT: "kubernetes"
  APPLICATION_NAME: "DevOps Deployment Dashboard"
  VERSION: "1.0"